JAVAC = javac
JAVA = java
JFLAGS = -d
all:

	$(JAVAC) $(JFLAGS) bin -classpath bin:third-party:. src/LoadBalancer.java
	$(JAVA) -cp "src:bin:third-party:third-party/*" LoadBalancer

clean:
	rm -rf bin/raytracer bin2/raytracer bin/BIT bin/*.class